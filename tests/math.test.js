test('adds 1 + 2 to equal 3', () => {
    expect((1+ 2)).toBe(3);
});

test('adds 5 + 7 to equal 12', () => {
    expect((5+ 7)).toBe(12);
});

test('adds -2 + 2 to equal 0', () => {
    expect((-2+ 2)).toBe(0);
});